  """ Información de Usuario """
  type Token {
    token: String!
  }

  type User {
      username: String
      password: String
  }

  type Message {
      message: String
  }

  """ Mutations  """
  type Mutation {
    auth(user: String!, password: String!): Token
    createUser(username: String!, password: String!): Message
  }

  type Query {
      getUser: User
  }
